NOTE:

se errore di cartella già esistente —> spark si disattiva

errore nell’esecuzione di dataset con sample in formato GTF

———————————

SELECT: manca controllo sulle regioni nel predicato (controllare parser)

PROJECT: metadata update non implementata

GROUP: non implementata

UNION: inserito left e right come prefisso, no il dataset


WEB SERVICE: manca regioni to Granges e upload function

MATERIALIZE: dove setto formato uscita 

TFAmr come fare?